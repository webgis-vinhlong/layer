# Đóng góp cho Vĩnh Long Layer Atlas

Cảm ơn bạn muốn cải thiện WebGIS. Dự án ưu tiên thay đổi nhỏ, có thể kiểm tra và giải thích rõ
tác động đến dữ liệu.

## Quy trình

1. Tạo issue mô tả lỗi hoặc đề xuất.
2. Fork repository và tạo nhánh theo dạng `feature/ten-ngan` hoặc `fix/ten-ngan`.
3. Chạy `npm install`, `npm run build:data` và `npm run verify`.
4. Nếu sửa GeoJSON, nêu rõ ID lớp, nguồn và lý do thay đổi.
5. Gửi pull request kèm ảnh trước/sau đối với thay đổi UI.

Không đưa thông tin cá nhân nhạy cảm, khóa API hoặc dữ liệu chưa được phép công bố vào repository.
Mã mới phải tương thích giấy phép MIT và giữ được trải nghiệm bàn phím/điện thoại.
